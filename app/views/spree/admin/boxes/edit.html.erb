<%= form_for [:admin, @box] do |f| %>
  <fieldset data-hook="edit_product_box">
    <legend align="center"><%= Spree.t(:edit_box) %></legend>

      <%= render :partial => 'form', :locals => { :f => f } %>

      <%= render partial: 'spree/admin/shared/edit_resource_links', locals: { collection_url: admin_boxes_url(@box) } %>
  </fieldset>
<% end %>