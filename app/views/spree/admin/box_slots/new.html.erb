<%= form_for [:admin, @box_slot], :html => { :multipart => true } do |f| %>
  <fieldset data-hook="new_box_slot">
    <legend align="center"><%= Spree.t(:new_box_slot) %></legend>

      <%= render :partial => 'form', :locals => { :f => f } %>

      <%= render partial: 'spree/admin/shared/new_resource_links', locals: { collection_url: admin_box_slots_url(@box_slot) } %>
  </fieldset>
<% end %>